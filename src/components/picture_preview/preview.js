class Preview {
  constructor (el, options) {
    const defaultOptions = {

    };
    this.$el = el;
    this.opions = Object.assign({}, defaultOptions, options);
  }
  init () {

  }
  create () {

  }
}